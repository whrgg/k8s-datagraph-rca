{
    "scenario": "Hotel reservation",
    "fault_type": "Container Kill",
    "aiopslab_command": "container_kill-detection",
    "app_name": "Hotel Reservation",
    "app_summary": "The application implements a hotel reservation service, built with Go and gRPC. The initial project is extended in several ways, including adding back-end in-memory and persistent databases, adding a recommender system for obtaining hotel recommendations, and adding the functionality to place a hotel reservation.",
    "target_namespace": "test-hotel-reservation",
    "service_starting_point": "frontend",
    "wait_before_launch_agent": 30,
    "target": "frontend",
    "RCA_gt": "The container process within the frontend pod was terminated and restarted by the Kubelet (Container Kill). Unlike a Pod Kill, the Kubernetes Pod object persists with the same name. The primary evidence is the Pod's \"RESTARTS\" count incrementing, while the Pod's total \"AGE\" remains unchanged.",
    "env_variables": {
        "JAEGER_SERVER_URL": "http://localhost:16686",
        "PROMETHEUS_SERVER_URL": "http://localhost:32000"
    },
    "execute": true
}